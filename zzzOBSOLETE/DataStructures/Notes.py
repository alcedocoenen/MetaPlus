# defining the Notes Pages


def makeNoteGroup():
    noteList = []
    return noteList


def makeNotesPage():
    noteGroupList = []
    return noteGroupList

# np = number of pages, nng = number of note groups, m = mode (0 = random, 1 = original score)


def makeNoteScore(np,nng,m):
    notesList = []
    return notesList

