# format1.py

# Contains the functions that transform format0 files into format1 files.
# format1 files contain the processing of the flags into pre-musical events.

# first function has to transform a format0c file into a format1a file
# which is a preparations for the processing of the flags.

